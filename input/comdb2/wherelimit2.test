  CREATE VIEW v1 AS SELECT a,b FROM t1;
  BEGIN;
  ROLLBACK;
  BEGIN;
  ROLLBACK;
  BEGIN;
  ROLLBACK;
  BEGIN;
  ROLLBACK;
    BEGIN;
    ROLLBACK;
    BEGIN;
    ROLLBACK;
    BEGIN;
    ROLLBACK;
    BEGIN;
    ROLLBACK;
    BEGIN;
    ROLLBACK;
  BEGIN;
  ROLLBACK;
  BEGIN;
  ROLLBACK;
  BEGIN;
  ROLLBACK;
  CREATE VIEW "v w" AS SELECT * FROM "x y";
