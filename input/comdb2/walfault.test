SELECT * FROM sqlite_master;
SELECT * FROM sqlite_master;
    BEGIN;
      CREATE TABLE x(y, z, UNIQUE(y, z));
    BEGIN;
    BEGIN;
      UPDATE z SET zzz = randomblob(799);
 INSERT INTO z VALUES(NULL, NULL);
    BEGIN;
      CREATE TABLE abc(a PRIMARY KEY);
      INSERT INTO abc SELECT randomblob(1500) FROM abc;
 SELECT * FROM sqlite_master;
    BEGIN;
      CREATE TABLE abc(a PRIMARY KEY);
    BEGIN;
      CREATE TABLE abc(a PRIMARY KEY);
    BEGIN;
      CREATE TABLE abc(a PRIMARY KEY);
    BEGIN;
      CREATE TABLE abc(a PRIMARY KEY);
    BEGIN;
    COMMIT;
    BEGIN;
    COMMIT;
