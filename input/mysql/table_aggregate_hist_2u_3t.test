# Tests for the performance schema   # table io and table lock by default  # hist by default  # 2users delete from performance_schema.setup_actors where user in ('user2', 'user4');
  # 3tables by default   