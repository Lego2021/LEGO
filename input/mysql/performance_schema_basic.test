 # # Only global #  select @@global.performance_schema;
  select @@session.performance_schema;
  show global variables like 'performance_schema';
  show session variables like 'performance_schema';
  select * from information_schema.global_variables where variable_name='performance_schema';
  select * from information_schema.session_variables where variable_name='performance_schema';
  # # Read-only #  set global performance_schema=1;
  set session performance_schema=1;
  