# Tests for sys schema # Verify the sys.schema_table_statistics view  # Ensure structure changes don't slip in DESC sys.schema_table_statistics;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.schema_table_statistics;
   # Ensure structure changes don't slip in DESC sys.x$schema_table_statistics;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.x$schema_table_statistics;
  