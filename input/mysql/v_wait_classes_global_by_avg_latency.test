# Tests for sys schema # Verify the sys.wait_classes_global_by_avg_latency view  # Ensure structure changes don't slip in DESC sys.wait_classes_global_by_avg_latency;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.wait_classes_global_by_avg_latency;
   # Ensure structure changes don't slip in DESC sys.x$wait_classes_global_by_avg_latency;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.x$wait_classes_global_by_avg_latency;
  