# # wl#7277: InnoDB: Bulk Load for Create Index #  # Test Restart & Crash Recovery. let $restart_noprint=2;
  # Test Row Format: REDUNDANT. let $row_format = REDUNDANT;
  # Test Row Format: COMPACT. let $row_format = COMPACT;
  # Test Row Format: DYNAMIC. let $row_format = DYNAMIC;
  # Test Row Format: COMPRESSED. let $row_format = COMPRESSED;
 