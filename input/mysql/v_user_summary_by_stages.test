# Tests for sys schema # Verify the sys.user_summary_by_stages view  # Ensure structure changes don't slip in DESC sys.user_summary_by_stages;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.user_summary_by_stages;
   # Ensure structure changes don't slip in DESC sys.x$user_summary_by_stages;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.x$user_summary_by_stages;
  