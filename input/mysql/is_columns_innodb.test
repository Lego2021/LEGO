# suite/funcs_1/t/is_columns_innodb.test # # Check the content of information_schema.columns about tables within # the databases created by the user. # Variant for storage engine InnoDB # # Author: # 2008-01-23 mleich WL#4203 Reorganize and fix the data dictionary tests of #                           testsuite funcs_1 #                   Create this script based on older scripts and new code. #  # This test cannot be used for the embedded server because we check here # privileges.  let $engine_type= InnoDB;
  # We look only for the tables created by datadict_load.inc. let $my_where = WHERE table_schema LIKE 'test%';
  