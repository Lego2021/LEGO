# Tests for sys schema # Verify the sys.user_summary_by_file_io view  # Ensure structure changes don't slip in DESC sys.user_summary_by_file_io;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.user_summary_by_file_io;
   # Ensure structure changes don't slip in DESC sys.x$user_summary_by_file_io;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.x$user_summary_by_file_io;
  