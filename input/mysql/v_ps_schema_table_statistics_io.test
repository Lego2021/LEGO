 DESC sys.x$ps_schema_table_statistics_io;
  DESC sys.x$ps_schema_table_statistics_io;
  # Ensure structure changes don't slip in DESC sys.x$ps_schema_table_statistics_io;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.x$ps_schema_table_statistics_io;
   # Ensure structure changes don't slip in DESC sys.x$ps_schema_table_statistics_io;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.x$ps_schema_table_statistics_io;
  