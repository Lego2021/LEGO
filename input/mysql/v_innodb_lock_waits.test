# Tests for sys schema # Verify the sys.innodb_lock_waits view  # Ensure structure changes don't slip in DESC sys.innodb_lock_waits;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.innodb_lock_waits;
   # Ensure structure changes don't slip in DESC sys.x$innodb_lock_waits;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.x$innodb_lock_waits;
  