# Tests for sys schema # Verify the sys.latest_file_io view  # Ensure structure changes don't slip in DESC sys.latest_file_io;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.latest_file_io;
   # Ensure structure changes don't slip in DESC sys.x$latest_file_io;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.x$latest_file_io;
  