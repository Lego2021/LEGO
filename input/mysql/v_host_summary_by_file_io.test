# Tests for sys schema # Verify the sys.host_summary_by_file_io view  # Ensure structure changes don't slip in DESC sys.host_summary_by_file_io;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.host_summary_by_file_io;
   # Ensure structure changes don't slip in DESC sys.x$host_summary_by_file_io;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.x$host_summary_by_file_io;
  