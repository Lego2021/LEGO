# # This test checks that qpress compression works with mariabackup # Initial SST happens via mariabackup, so there is not much to do in the body # of the test #   SELECT 1;
   # Confirm that transfer was compressed using qpress algorithm 