#### suite/funcs_1/t/myisam_trig_03e.test  # This test cannot be used for the embedded server because we check here # privileges.  # MyISAM tables should be used # # Set $engine_type SET @@session.sql_mode = 'NO_ENGINE_SUBSTITUTION';
 let $engine_type= myisam;
  # Create some objects needed in many testcases USE test;
   