# Tests for sys schema # Verify the sys.schema_unused_indexes view  # Ensure structure changes don't slip in DESC sys.schema_unused_indexes;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.schema_unused_indexes;
  