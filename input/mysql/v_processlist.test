# Tests for sys schema # Verify the sys.processlist view  # Ensure structure changes don't slip in DESC sys.processlist;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.processlist;
   # Ensure structure changes don't slip in DESC sys.x$processlist;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.x$processlist;
  