# Tests for sys schema # Verify the sys.host_summary_by_statement_latency view  # Ensure structure changes don't slip in DESC sys.host_summary_by_statement_latency;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.host_summary_by_statement_latency;
   # Ensure structure changes don't slip in DESC sys.x$host_summary_by_statement_latency;
  # Make sure view select does not error, but ignore results SELECT * FROM sys.x$host_summary_by_statement_latency;
  