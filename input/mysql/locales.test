if (`select count(*) = 0 from information_schema.plugins where plugin_name = 'locales' and plugin_status='active'`) { }  select * from information_schema.locales;
 show locales;
 show locales like '%spanish%';
 show locales where description like '%spanish%';
 flush locales;
  